## Agent skills

### Issue tracker

Issues are tracked in GitHub Issues (`gh` CLI) for `chitaolang/learnwhile`. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
